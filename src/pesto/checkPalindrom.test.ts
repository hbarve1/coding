/** @format */

import checkPalindrom from "./checkPalindrom";

describe("", () => {
  test("", () => {
    expect(checkPalindrom("aba")).toStrictEqual(true);

    expect(checkPalindrom("abab")).toStrictEqual(false);
  });
});
