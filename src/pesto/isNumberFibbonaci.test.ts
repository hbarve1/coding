/** @format */

import isNumberFibbonaci from "./isNumberFibbonaci";

describe("", () => {
  test("", () => {
    expect(isNumberFibbonaci(8)).toBe(true);
    expect(isNumberFibbonaci(10)).toBe(false);
  });
});
