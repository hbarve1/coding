/** @format */

import matchingPair from "./matching-pair";

describe("", () => {
  test("", () => {
    expect(matchingPair(1)).toBe(2);
    expect(matchingPair(10)).toBe(11);
    expect(matchingPair(8)).toBe(9);
    expect(matchingPair(2)).toBe(3);
  });
});
