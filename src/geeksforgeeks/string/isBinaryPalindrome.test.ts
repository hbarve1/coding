/** @format */

import isBinaryPalindrome from "./isBinaryPalindrome";

describe("", () => {
  test("", () => {
    expect(isBinaryPalindrome(5)).toBe(1);
    expect(isBinaryPalindrome(10)).toBe(0);
  });
});
