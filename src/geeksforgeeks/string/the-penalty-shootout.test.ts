/** @format */

import isPalindrome from "./the-penalty-shootout";

describe("", () => {
  test("", () => {
    expect(isPalindrome("1012012112110")).toBe(2);
    expect(isPalindrome("111122")).toBe(0);
  });
});
