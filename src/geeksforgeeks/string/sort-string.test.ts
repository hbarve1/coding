/** @format */

import sortString from "./sort-string";

describe("", () => {
  test("", () => {
    expect(sortString(2, ["bbccdefbbaa", "geeksforgeeks"])).toStrictEqual([
      "aabbbbccdef",
      "eeeefggkkorss",
    ]);
  });
});
