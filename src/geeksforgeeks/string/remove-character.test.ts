/** @format */

import removeCharacter from "./remove-character";

describe("", () => {
  test("", () => {
    expect(removeCharacter("computer", "cat")).toBe("ompuer");
    expect(removeCharacter("occurrence", "car")).toBe("ouene");
  });
});
