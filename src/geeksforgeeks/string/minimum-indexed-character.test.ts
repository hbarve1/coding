/** @format */

import minimumIndexedCharacter from "./minimum-indexed-character";

describe("", () => {
  test("", () => {
    expect(minimumIndexedCharacter("zsyle", "bjz")).toBe("z");
    expect(minimumIndexedCharacter("anskg", "me")).toBe("$");
  });
});
